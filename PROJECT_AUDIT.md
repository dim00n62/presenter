# 🔍 Аудит проекта Presentation Agent

## 📊 Общая статистика

- **Backend файлов**: ~30
- **Frontend файлов**: ~15
- **Мертвого кода**: ~6 файлов/компонентов
- **TODO/FIXME**: 4 места
- **Дублирование логики**: 3 случая

---

## 🔴 Критические проблемы

### 1. **Workflow Manager НЕ используется**

**Файл**: `backend/src/agents/workflow.ts` (216 строк)

**Проблема**: 
- Создан полноценный `WorkflowManager` с LangGraph
- Экспортируется `workflowManager`
- **НИ РАЗУ не используется в проекте!**

**Код**:
```typescript
export const workflowManager = new WorkflowManager();
```

**Что делать**:
```bash
# УДАЛИТЬ полностью, если не планируется использовать
rm backend/src/agents/workflow.ts
```

**Причина**: С новым пошаговым workflow (ProjectPageV2) этот автоматический workflow вообще не нужен.

---

### 2. **Дублирование Health Check**

**Файл**: `backend/src/index.ts`

**Проблема**: Health check определен ДВА раза:
```typescript
// Строка 36
app.use('/health', healthRouter);

// Строка 39 - ДУБЛИКАТ!
app.get('/health', (req, res) => {
    res.json({ ... });
});
```

**Что делать**:
```typescript
// УДАЛИТЬ строки 39-48 в index.ts
// Оставить только:
app.use('/health', healthRouter);
```

---

### 3. **TODO с незавершенной функциональностью**

#### a) Регенерация слайдов в Blueprint
**Файл**: `backend/src/routes/blueprints.ts:177`

```typescript
// TODO: Implement regeneration logic
// For now, just add a note
blueprint.slides[slideIndex].description += ` [Обновлено: ${userFeedback}]`;
```

**Проблема**: Endpoint `/regenerate-slide` существует, но НЕ регенерирует, а просто добавляет текст.

**Что делать**:
- Либо реализовать полноценно (вызвать blueprint-agent)
- Либо УДАЛИТЬ endpoint (строки 154-189)

#### b) Streaming не реализован
**Файл**: `backend/src/services/qwen-client.ts:411`

```typescript
async chatStream() {
    // TODO: Implement streaming if Qwen API supports it
    throw new Error('Streaming not yet implemented');
}
```

**Что делать**: 
- Если не нужен - УДАЛИТЬ метод (строки 406-413)
- Если нужен - реализовать

---

## 🟡 Мертвый код (неиспользуемые компоненты)

### 1. **RAGTest Component**

**Файл**: `frontend/src/components/RAGTest.tsx` (62 строки)

**Проблема**: 
- Компонент создан
- **НЕ импортируется нигде**
- НЕ используется в UI

**Что делать**:
```bash
rm frontend/src/components/RAGTest.tsx
```

**Или** добавить в ProjectPage для тестирования:
```typescript
<Tab key="rag-test" title="🔍 RAG Test">
  <RAGTest projectId={projectId} />
</Tab>
```

---

### 2. **ProjectList Component**

**Файл**: `frontend/src/components/ProjectList.tsx` (121 строка)

**Проблема**:
- Компонент для списка проектов
- **НЕ используется** (HomePage использует собственную логику)

**Что делать**:
```bash
rm frontend/src/components/ProjectList.tsx
```

Или использовать вместо дублирования в HomePage.

---

### 3. **DocumentList Component**

**Файл**: `frontend/src/components/DocumentList.tsx` (110 строк)

**Проблема**:
- Компонент для списка документов
- **НЕ используется** (ProjectPage показывает документы inline)

**Что делать**:
```bash
rm frontend/src/components/DocumentList.tsx
```

---

### 4. **ProjectProgress Component**

**Файл**: `frontend/src/components/ProjectProgress.tsx` (57 строк)

**Проблема**:
- Красивый прогресс-бар
- **НЕ используется** (ProjectPage не показывает прогресс)
- С новым WorkflowStepper вообще не нужен

**Что делать**:
```bash
rm frontend/src/components/ProjectProgress.tsx
```

---

### 5. **ProjectSettings Component**

**Файл**: `frontend/src/components/ProjectSettings.tsx` (73 строки)

**Проблема**:
- Модальное окно для редактирования целей
- **НЕ используется** нигде

**Что делать**:
Либо УДАЛИТЬ:
```bash
rm frontend/src/components/ProjectSettings.tsx
```

Либо добавить в ProjectPage:
```typescript
<ProjectSettings project={project} onUpdate={loadProject} />
```

---

### 6. **useProjectStatus Hook**

**Файл**: `frontend/src/hooks/useProjectStatus.ts`

Нужно проверить, используется ли. Если нет - удалить.

---

## 🟢 Рекомендации по улучшению

### 1. **API методы - дублирование**

**Проблема**: В `lib/api.ts` могут быть неиспользуемые методы для старого workflow.

**Что проверить**:
```typescript
// Эти методы могут быть не нужны:
askQuestion()  // Если RAGTest удален
// ... другие
```

**Действие**: Проверить использование каждого метода.

---

### 2. **Константы типов**

**Файл**: `frontend/src/components/ProjectList.tsx:6-13`

```typescript
export type ProjectStatus =
    | 'created'
    | 'parsed'
    | 'analyzed'
    | 'blueprint_ready'
    | 'content_generated'
    | 'presentation_ready'
    | 'error';
```

**Проблема**: Дублируется с типами в database.ts

**Что делать**: Создать общий файл типов:
```typescript
// frontend/src/types/project.ts
export type ProjectStatus = ...
```

И импортировать везде.

---

### 3. **Обработка ошибок**

**Проблема**: Много мест с `alert()`:
```typescript
alert('Ошибка: ' + error.message);
```

**Что делать**: Использовать toast/notification библиотеку:
```bash
npm install sonner
```

```typescript
import { toast } from 'sonner';

toast.error('Ошибка: ' + error.message);
```

---

### 4. **Hardcoded strings**

**Проблема**: Много хардкода строк на русском:
```typescript
alert('✅ Настройки сохранены');
```

**Что делать**: Создать i18n или хотя бы константы:
```typescript
// constants/messages.ts
export const MESSAGES = {
  SETTINGS_SAVED: '✅ Настройки сохранены',
  // ...
}
```

---

## 📋 План действий по очистке

### Шаг 1: Удалить мертвый код (5 минут)

```bash
# Backend
rm backend/src/agents/workflow.ts

# Frontend
rm frontend/src/components/RAGTest.tsx
rm frontend/src/components/ProjectList.tsx
rm frontend/src/components/DocumentList.tsx
rm frontend/src/components/ProjectProgress.tsx
rm frontend/src/components/ProjectSettings.tsx
```

### Шаг 2: Исправить дублирование (2 минуты)

**В `backend/src/index.ts`** - удалить строки 39-48:
```typescript
// Удалить это:
app.get('/health', (req, res) => {
    res.json({
        status: 'ok',
        // ...
    });
});
```

### Шаг 3: Удалить или реализовать TODO (10 минут)

#### Вариант A: Удалить недоделанное

**В `backend/src/routes/blueprints.ts`** - удалить endpoint:
```typescript
// УДАЛИТЬ строки 154-189
blueprintsRouter.post('/:id/regenerate-slide', ...);
```

**В `backend/src/services/qwen-client.ts`** - удалить метод:
```typescript
// УДАЛИТЬ строки 406-413
async chatStream() { throw new Error(...); }
```

**В `backend/src/agents/blueprint-agent.ts`** - удалить TODO комментарий:
```typescript
// УДАЛИТЬ строку с TODO
```

#### Вариант B: Реализовать

Если хотите реализовать - нужно:
1. `regenerate-slide` - вызвать `blueprintAgent.regenerateSlide()`
2. `chatStream` - реализовать SSE streaming

### Шаг 4: Проверить imports (3 минуты)

```bash
# Проверить что ничего не сломалось
cd backend && npm run build
cd frontend && npm run build
```

### Шаг 5: Обновить package.json (опционально)

Если были удалены зависимости:
```bash
npm prune
```

---

## 📊 Результаты очистки

### До:
- **Backend**: ~30 файлов
- **Frontend**: ~15 файлов
- **Мертвого кода**: ~6 компонентов + 1 workflow manager
- **Незавершенного кода**: 4 TODO

### После:
- **Backend**: ~29 файлов (-1 workflow)
- **Frontend**: ~10 файлов (-5 мертвых компонентов)
- **Мертвого кода**: 0
- **Незавершенного кода**: 0

**Экономия**: ~500 строк кода, улучшение maintainability

---

## 🔧 Дополнительные улучшения (опционально)

### 1. TypeScript strict mode

В `tsconfig.json`:
```json
{
  "compilerOptions": {
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true
  }
}
```

Это выявит неиспользуемые переменные автоматически.

### 2. ESLint правила

```json
{
  "rules": {
    "no-unused-vars": "error",
    "@typescript-eslint/no-unused-vars": "error"
  }
}
```

### 3. Dependency audit

```bash
# Проверить устаревшие зависимости
npm outdated

# Проверить уязвимости
npm audit
```

---

## ✅ Checklist для очистки

- [ ] Удалить `workflow.ts` (не используется)
- [ ] Удалить дубликат health check в `index.ts`
- [ ] Удалить 5 неиспользуемых компонентов (RAGTest, ProjectList, DocumentList, ProjectProgress, ProjectSettings)
- [ ] Удалить или реализовать TODO в `blueprints.ts`
- [ ] Удалить или реализовать `chatStream` в `qwen-client.ts`
- [ ] Проверить компиляцию после удаления
- [ ] Запустить проект и протестировать
- [ ] Обновить документацию (если есть)

---

## 🎯 Итоговые рекомендации

### Must do (обязательно):
1. ✅ Удалить мертвый код (6 файлов)
2. ✅ Исправить дубликат health check
3. ✅ Убрать TODO с фейковой реализацией

### Should do (желательно):
4. 📝 Вынести типы в отдельные файлы
5. 🔔 Заменить alert() на toast notifications
6. 🌐 Создать константы для сообщений

### Nice to have (по желанию):
7. 📏 Включить strict mode TypeScript
8. 🔍 Настроить ESLint правила
9. 🔒 Провести security audit

---

## 📞 Вопросы для обсуждения

1. **Workflow Manager**: Планировалось ли его использовать? Может пригодиться для будущего?
2. **RAG Test**: Нужен ли для дебага? Или можно удалять?
3. **Streaming**: Будет ли реализован или можно удалить метод?
4. **Неиспользуемые компоненты**: Были для старой версии UI или планировалось использовать?

---

**Автор аудита**: Claude  
**Дата**: 13 января 2026  
**Версия**: 1.0
